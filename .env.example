# ProEx Platform Environment Variables
# Copy this file to .env and fill in your actual values

# =============================================================================
# LLM API Configuration
# =============================================================================

# OpenRouter API Key (REQUIRED)
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# =============================================================================
# Logo Scraping API
# =============================================================================

# Logo.dev API Key (OPTIONAL)
# Get your API key from: https://logo.dev
# Falls back to hardcoded key if not set
LOGODEV_API_KEY=your_logodev_api_key_here

# =============================================================================
# Email Service Configuration
# =============================================================================

# Email Service URL (OPTIONAL)
# Default: http://localhost:3001
# For production: Set to your deployed email service URL
EMAIL_SERVICE_URL=http://localhost:3001

# =============================================================================
# CORS Configuration
# =============================================================================

# Allowed CORS Origins (OPTIONAL)
# Comma-separated list of allowed origins
# Default: http://localhost:3000,http://localhost:5173
# For production: Set to your actual frontend domain(s)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# =============================================================================
# Supabase Configuration (DEPRECATED - Currently Non-Functional)
# =============================================================================

# Supabase Project ID (OPTIONAL - DO NOT USE)
# Note: Supabase integration is currently disabled and non-functional
# Only set if you've implemented proper Supabase integration
SUPABASE_PROJECT_ID=your_supabase_project_id

# Enable Supabase Integration (OPTIONAL - DO NOT USE)
# Set to "true" to enable experimental Supabase features (not recommended)
# Default: false
ENABLE_SUPABASE=false

# =============================================================================
# Performance Optimization
# =============================================================================

# Enable Embedding Generation (OPTIONAL)
# Set to "true" to enable ML embeddings for similarity analysis
# WARNING: Adds ~5-10 seconds per letter - DISABLE for faster processing
# Default: false (disabled for performance)
ENABLE_EMBEDDINGS=false

# =============================================================================
# Development vs Production
# =============================================================================

# For Development:
# - Use default values (mostly localhost)
# - OPENROUTER_API_KEY is still required

# For Production:
# - Set all URLs to production values
# - Use strong API keys
# - Set CORS_ORIGINS to your actual frontend domain
# - Consider migrating from Replit to AWS/GCP/Azure for better security

# =============================================================================
# Notes
# =============================================================================

# 1. Never commit .env file to git (it's in .gitignore)
# 2. Always use .env.example as template
# 3. Supabase integration is currently non-functional - don't enable it
# 4. For production, use proper secrets management (e.g., AWS Secrets Manager)
